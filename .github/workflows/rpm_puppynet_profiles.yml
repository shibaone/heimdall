name: packager_puppynet_rpm_profiles

on:
  push:
    branches:
      - 'master'
    paths:
      - '**'
    tags:
      - 'v*.*.*'
      - 'v*.*.*-*'

jobs:
  build:
    permissions:
      id-token: write
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Adding TAG to ENV
        run: echo "GIT_TAG=`echo $(git describe --tags --abbrev=0)`" >> $GITHUB_ENV

      - name: Adding TAG1 to ENV
        run: echo "GIT_TAG1=`echo $(git describe --tags --abbrev=0)`" | sed 's/-/./g' >> $GITHUB_ENV

      - name: Installing some dependencies
        run: sudo apt-get update && sudo apt-get install -y rpm

      - name: echo some tags
        run: echo ${{ env.GIT_TAG1 }} and ${{ env.GIT_TAG }}

      - name: Setup rpm package directories
        run: |
          mkdir -p packaging/rpm/SPECS
          mkdir -p packaging/rpm/BUILD
          mkdir -p packaging/rpm/RPMS
          mkdir -p packaging/rpm/SRPMS

      - name: Start tasks needed for Puppynet Sentry node
        run: echo "#########################################################################"

      - name: Setting up Puppynet Sentry Node Spec files
        run: |
          touch packaging/rpm/heimdall-puppynet-sentry.spec
          echo "Name: heimdall-puppynet-sentry-config" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "Version: ${{ env.GIT_TAG1 }}" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "Release:  1%{?dist}" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "License: GPL/AGPL" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "BuildArch: noarch" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "Summary: heimdall puppynet sentry config rpm package" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "%description" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "heimdall puppynet Sentry rpm package" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "%pre" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "sudo -u heimdall heimdalld init --chain=puppynet --home /var/lib/heimdall" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "%install" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "mkdir -p %{buildroot}/var/lib/heimdall/config" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "mkdir -p %{buildroot}/lib/systemd/system" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/systemd/heimdalld-puppynet-sentry.service %{buildroot}/lib/systemd/system/heimdalld.service" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/config/puppynet/config.toml %{buildroot}/var/lib/heimdall/config/config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/config/puppynet/heimdall-config.toml %{buildroot}/var/lib/heimdall/config/heimdall-config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "%files" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "/lib/systemd/system/heimdalld.service" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "/var/lib/heimdall/config/config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "/var/lib/heimdall/config/heimdall-config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "%attr(0755, heimdall, heimdall) /var/lib/heimdall" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "%post" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "/bin/systemctl daemon-reload" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

      - name: Build puppynet Sentry Node profile RPM
        run: |
          rpmbuild --define "_topdir /home/runner/work/heimdall/heimdall/packaging/rpm_build" \
           --define "_builddir %{_topdir}/BUILD" \
           --define "_rpmdir %{_topdir}/RPMS" \
           --define "_srcrpmdir %{_topdir}/SRPMS" \
           --define "__spec_install_post /bin/true" \
           -bb packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

      - name: Setting up puppynet Validator Node node
        run: echo "#########################################################################"

      - name: Setting up puppynet Validator Node Spec files
        run: |
          touch packaging/rpm/heimdall-puppynet-validator.spec
          echo "Name: heimdall-puppynet-validator-config" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "Version: ${{ env.GIT_TAG1 }}" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "Release:  1%{?dist}" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "License: GPL/AGPL" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "BuildArch: noarch" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "Summary: heimdall puppynet validator config rpm package" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec

          echo "%description" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "heimdall puppynet Validator rpm package" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec

          echo "" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "%pre" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "sudo -u heimdall heimdalld init --chain=puppynet --home /var/lib/heimdall" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "%install" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "mkdir -p %{buildroot}/var/lib/heimdall" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "mkdir -p %{buildroot}/lib/systemd/system" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/systemd/heimdalld-puppynet-validator.service %{buildroot}/lib/systemd/system/heimdalld.service" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/config/puppynet/config.toml %{buildroot}/var/lib/heimdall/config/config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "cp /home/runner/work/heimdall/heimdall/packaging/templates/config/puppynet/heimdall-config.toml %{buildroot}/var/lib/heimdall/config/heimdall-config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "%files" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "/lib/systemd/system/heimdalld.service" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "/var/lib/heimdall/config/config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec
          echo "/var/lib/heimdall/config/heimdall-config.toml" >> packaging/rpm/SPECS/heimdall-puppynet-sentry.spec

          echo "%attr(0755, heimdall, heimdall) /var/lib/heimdall" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "%post" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec
          echo "/bin/systemctl daemon-reload" >> packaging/rpm/SPECS/heimdall-puppynet-validator.spec

      - name: Build puppynet Validator Node profile RPM
        run: |
          rpmbuild --define "_topdir /home/runner/work/heimdall/heimdall/packaging/rpm_build" \
           --define "_builddir %{_topdir}/BUILD" \
           --define "_rpmdir %{_topdir}/RPMS" \
           --define "_srcrpmdir %{_topdir}/SRPMS" \
           --define "__spec_install_post /bin/true" \
           -bb packaging/rpm/SPECS/heimdall-puppynet-validator.spec

      - name: rename packages due to rpm dist rules puppynet validator
        run: mv /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-validator-config-${{ env.GIT_TAG1 }}-1.noarch.rpm /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-validator-config-${{ env.GIT_TAG1 }}.noarch.rpm
      - name: rename packages due to rpm dist rules puppynet sentry
        run: mv /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-sentry-config-${{ env.GIT_TAG1 }}-1.noarch.rpm /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-sentry-config-${{ env.GIT_TAG1 }}.noarch.rpm

      - name: shasum a package puppynet validator
        run: shasum  /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-validator-config-${{ env.GIT_TAG1 }}.noarch.rpm > /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-validator-config-${{ env.GIT_TAG1 }}.noarch.rpm.checksum
      - name: shasum a package puppynet sentry
        run: shasum  /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-sentry-config-${{ env.GIT_TAG1 }}.noarch.rpm > /home/runner/work/heimdall/heimdall/packaging/rpm_build/RPMS/noarch/heimdall-puppynet-sentry-config-${{ env.GIT_TAG1 }}.noarch.rpm.checksum

      - name: Release heimdall Packages
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.GIT_TAG }}
          prerelease: true
          files: |
            packaging/rpm_build/RPMS/noarch/heimdall-puppynet**.rpm
            packaging/rpm_build/RPMS/noarch/heimdall-puppynet**.rpm.checksum